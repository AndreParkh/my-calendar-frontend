// import axios from 'axios'
// import { AUTH_TOKEN, TYPE_TOKEN } from '@/constants/constants.ts'
//
// const apiClient = axios.create({
//   baseURL: '/',
// })
//
// apiClient.interceptors.request.use((config) => {
//   const token = (AUTH_TOKEN)
//   if (token) config.headers.Authorization = `${TYPE_TOKEN} ${token}`
//   return config
// })
